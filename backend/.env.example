# Existing Variables
PORT=5000
MONGO_URI=mongodb+srv://sardarabhishek14_db_user:namaste123@namaste-restro.8npqagz.mongodb.net/?appName=namaste-restro
JWT_SECRET=namaste_restro_secret_key_2024

# NEW: Stripe Configuration
# ===========================

# Generate ENCRYPTION_KEY using:
# node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
# Example: 
ENCRYPTION_KEY=your_32_character_hex_string_here

# Your domain (used for webhook URL generation)
# For development:
DOMAIN=http://localhost:5000
# For production:
# DOMAIN=https://yourdomain.com

# Stripe Keys (get from dashboard.stripe.com)
# Note: Keep these secret! Use the Admin Stripe Settings endpoint to add them
# STRIPE_PUBLIC_KEY=pk_test_xxxx  (Don't add to .env for production)
# STRIPE_SECRET_KEY=sk_test_xxxx  (Don't add to .env for production)
# STRIPE_WEBHOOK_SECRET=whsec_xxxx (Don't add to .env for production)
